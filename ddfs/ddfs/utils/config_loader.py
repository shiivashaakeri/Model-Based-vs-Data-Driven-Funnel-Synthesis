"""Configuration loading utilities.

This module provides the load_config function for loading configuration
files (typically YAML) and parsing them into configuration objects.
"""


def load_config(config_path):
    """Load configuration from a file.
    
    Args:
        config_path: Path to the configuration file (typically YAML).
        
    Returns:
        Configuration object or dictionary containing parsed configuration.
    """
    pass

